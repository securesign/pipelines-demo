apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: el-securesign-event-listener
spec:
  host: el-securesign-event-listener.apps.open-svc-sts.k1wl.p1.openshiftapps.com
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-securesign-event-listener 
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
